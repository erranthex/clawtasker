# ⚠️ GENERATED — Do Not Edit

Files in `ui/dist/` are **build output** generated by:

```bash
python3 scripts/build_ui.py
```

**To make changes**, edit files in `ui/src/modules/` and rebuild.

This directory is committed so the app works out-of-the-box,
but PRs should not include manual edits to these files.
